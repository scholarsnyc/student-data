%h1= @title || "Students"

= partial 'students/grades'

%table{class: "table"}
  %thead
    %tr
      %th First Name
      %th Last Name
      %th Homeroom
      %th Grade
  %tbody
  - @students.each do |student|
    %tr{"data-grade" => student.grade, "data-homeroom" => student.homeroom}
      %td= link_to student.first_name, url_for(:students, :show, :id => student.id)
      %td= link_to student.last_name, url_for(:students, :show, :id => student.id)
      %td= student.grade
      %td= link_to_homeroom student.homeroom
