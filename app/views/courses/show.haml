%h1= @course.name

%table.table
  %thead
    %tr
      %th Section
      %th Marking Period
      %th Year
      %th Weight
    %tr
      %td= @course.section
      %td= @course.marking_period
      %td= @course.year
      %td= @course.weight

%h2 Grades
%table{class: "table"}
  %thead
    %tr
      %th Student
      %th Homeroom
      %th Score
      %th Benchmark
  %tbody
  - @records.each do |record|
    %tr
      %td= link_to record.student.name, url_for(:students, :show, :id => record.student.id)
      %td= link_to_homeroom record.student.homeroom
      %td= record.score
      %td= record.exam
